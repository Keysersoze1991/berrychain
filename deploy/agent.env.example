# /etc/berrychain/agent.env (root and berry only). The agent needs a model API key
# and the passphrase of its (encrypted) wallet. Neither is ever written to disk by the agent.
ANTHROPIC_API_KEY=sk-ant-...
BERRY_WALLET_PASSPHRASE=...
